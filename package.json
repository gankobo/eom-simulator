{
  "name": "eom-simulator",
  "version": "0.1.0",
  "description": "外眼筋作用シミュレータ — 眼位依存の6筋作用を計算・可視化（Tier0 kinematic / Tier1 string model）",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
